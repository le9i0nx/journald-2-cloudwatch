language: python
dist: trusty
sudo: false
services:
    - docker

before_install:
    - make docker_build
install:
    - make docker_build_test

script:
    - make docker_test

after_success:
    - bash <(curl -s https://codecov.io/bash) -X gcov

notifications:
    email: false
